import { Inventory } from "@warehouse/core";
import config from "../config";

const seedInventory = () => {
  console.log(config);
};

const seedProduct = () => {
  console.log(config);
};

export default {
  seedInventory,
  seedProduct,
};
